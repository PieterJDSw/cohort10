# MVP Assessment

Project structure:

- `backend/`: FastAPI, SQLAlchemy, Alembic-style startup migrations, CrewAI-ready evaluator flow
- `frontend/`: React + Vite assessment UI
- `docs/`: startup and run instructions
- `infra/`: reserved for future infrastructure additions
